import { Plugin } from '/Users/mrsingsing/mrsingsing/CSS-GuideBook/node_modules/@umijs/runtime';

const plugin = new Plugin({
  validKeys: ['patchRoutes','rootContainer','render','onRouteChange',],
});

export { plugin };
